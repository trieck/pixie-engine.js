var settings = {};

settings.content = {
	repository: './content'
};

settings.index = {
	// number of ways to merge
	nway: 20
};

settings.inverter = {
	// maximum number of inverter records
	maxrecs: 100000
};

module.exports = settings;